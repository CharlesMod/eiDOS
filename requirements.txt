tomli>=2.0.0; python_version < "3.11"
pydantic>=2.12
pydantic-settings>=2.7
pyyaml>=6.0
rank-bm25>=0.2.2
numpy>=1.24
pyzmq>=26   # V3 nervous-system bus: cross-process/device transport (ZmqTransport)
bleak>=0.22 # M4 metabolism: read the Renogy MPPT/inverter over BLE (only used if [nervous].power_enabled)
# Optional — only needed if knowledge.embedding_enabled = true:
# onnxruntime>=1.16
# tokenizers>=0.15
